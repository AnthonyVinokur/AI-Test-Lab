# ATL-A.11.02 — Strict Translation and Validation Boundary

Translates only the exact supported schema into trusted entries. Missing/additional fields, malformed
identifiers, non-canonical UTC timestamps or bytes, and unknown versions or algorithms produce one stable
safe document error.

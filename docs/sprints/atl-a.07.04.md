# ATL-A.07.04 — Deterministic Package Manifest

Builds a canonical manifest binding version, package and ledger IDs, ordered evidence
IDs, run and admission references, sequence boundaries, count, algorithm, chain head,
selection, metadata, and content digest. Package identity excludes volatile build time.

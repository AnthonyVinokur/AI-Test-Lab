# ATL-A.06.09 — Public Ledger Outcome and Serialization

An explicit public projection exposes only approved ledger facts. Sorted compact JSON is
deterministic and round-trippable. Keys, signatures, policy objects, storage details,
tracebacks, signer resolution, and internal algorithms never cross the export boundary.

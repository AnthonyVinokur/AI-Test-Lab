# ATL-A.07.08 — Independent Package Verification

Verifies public JSON without ledger or database access. It checks canonical encoding,
version and algorithm, package and content digests, manifest bindings, selection
completeness, record identities, entry digests, sequence, chain, run, admission, and
provenance-bound fields.

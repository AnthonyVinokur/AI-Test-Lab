# ATL-A.09.03 — Disclosure Scope and Field Allowlisting

Uses an explicit public record-field allowlist. Record and field requests must be subsets of both the
verified package and authorization scope; unknown fields fail closed.

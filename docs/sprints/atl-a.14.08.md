# ATL-A.14.08 — Immutable Deployment Authorization Attestation

Creates a frozen canonical authorization binding artifact, deployment, upstream identities and digests, authorization policy, conditions, validity, issuer/key reference, correlation, payload digest, and Ed25519-compatible signature. Any change requires a new attestation and invalidates the old integrity proof.
